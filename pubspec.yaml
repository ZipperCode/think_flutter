name: think_flutter
description: A new Flutter application.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  cupertino_icons: ^1.0.0
  json_annotation: ^2.0.0
  sqflite: ^1.3.1
  xml:
#  code_generator:
#    path: ./code_generator

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.0.0
  json_serializable: ^2.0.0
  
flutter:
  uses-material-design: true
  
  assets:
    - assets/db_config.xml
    - assets/images/
    - assets/o.svg
